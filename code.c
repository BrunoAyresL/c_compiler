
int func(int a, int b, int c, int d) {
    int x = a + b;
    int y = b + c;
    int z = c + d;
    int w = x + y;
    int u = y + z;
    int v = w + u;
    int t = x + z;
    int r = t + v;
    int s = r + y;
    int q = s + u;
    int p = q + x;

    if (p == q + x) {
        if (x + b < 8) {
            int aa = a + b + q + s;
            if (p > 1) {
                return x;
            } else {
                int xx = x - y;
            }
        } else {
            r = -2;
        }
     } else if (r > 0) {
        return w;
     } else {
        s = 0;
     }

    return p;
}

int main() {
    int x = 0;
    int y = 10;
    int s = 25;

    while (x < 0) {
        int l = 25;
        l = l + x + 2;
        x = x + 1;
    }

    char c = 'h';
    char d = 'i';
    if (c == d) {
        x = 1;
        if (c == d) {
            x = 2;
            if (c == d) {
                x = 3;
                if (c == d) {
                    x = 4;
                }
            }
        }
    }

    for (int i = 0; i < 20; i = i + 1) {
        x = x + 2;
    }

    if (x + 5 / 3) {
        y = y + 2;
        x = y * 5;
        s = 200;
    } else if (y < 2) {
        x = 10000 % y << s;
    } else {
        y = 999;
        int h = x + y;
    }
    func(x-2, 2, 18, 1);
}
